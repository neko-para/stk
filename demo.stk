load "base.so";
load "io.so";
using base;
using io;
call {start};
jmp {end};
fact:
	cmp [-2], 1;
	jle {fact_end};
	push [-2] - 1;
	call {fact};
	mov 1, [1] * [-2];
	ret 1;
fact_end:
	mov 1, 1;
	ret 1;

start:
	push 10;
	call {fact};
	outs "The fact of 10 is ";
	out [1];
	outs "\n";
	ret;
end:
